rules_version = '2';
// Deploy with: firebase deploy --only storage
// content/* = project images. Public read, write when signed in to control-centre.
service firebase.storage {
  match /b/{bucket}/o {
    match /content/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
